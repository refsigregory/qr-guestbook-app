// utils/accessCodeGenerator.js
import { v4 as uuidv4 } from 'uuid'; // Install this package if you haven't already

export function generateUniqueAccessCode() {
  // Generate a unique code (e.g., UUID or any custom logic)
  return uuidv4();
}
